{
  "key": "TASK-066",
  "title": "Preview process manager — spawn/stop/restart + bounded log capture + URL detection",
  "description": "BETA-TEST GOAL (goal-app-preview), part 2 of 5. A managed child-process controller for the previewed app. Starts the resolved command as a child process, captures stdout/stderr into a BOUNDED ring buffer, tracks a state machine, detects the served URL/port (from config or by scanning early logs), and stops/restarts cleanly. Single active preview at a time. Mirrors the orchestrator-bridge (src/orchestrator-bridge.js) spawn discipline. Independent of TASK-065 (tested with a hand-built config object); TASK-067 wires the two together.",
  "acceptance_criteria": [
    "New module src/preview-process.js exports a controller (e.g. createPreviewController({repoRoot}) or start/stop/restart/getStatus) managing ONE child process launched from a resolved preview config.",
    "State machine: stopped -> starting -> running (or -> exited/error); getStatus returns { state, mode, url (string|null), pid (number|null), recentLogs (bounded array) }.",
    "stdout/stderr captured into a BOUNDED buffer (named cap constant on lines and/or bytes, oldest dropped, no unbounded growth); never throws on odd/binary output.",
    "URL/port detection: use the configured url/port if present; else scan early stdout for the first localhost URL or a listening-on-PORT pattern and surface it in status.",
    "stop() terminates the child cleanly with no orphan process; restart() = stop then start; stop() is idempotent when already stopped (no throw).",
    "tdd: failing tests first for state-machine transitions, buffer bounding, URL detection, and idempotent stop, exercised with a trivial spawnable fixture (tiny node http server / echo loop) under tests/e2e/.",
    "release-tier gate: npm run test:all green; rebuild + commit dist if bundled source changes."
  ],
  "status": "done",
  "priority": "high",
  "labels": [
    "beta",
    "goal-app-preview",
    "console",
    "preview"
  ],
  "assignee": null,
  "depends_on": [],
  "linked_commits": [
    "a15dfed",
    "46427be",
    "55dfd70"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-06-17T03:40:59.236Z",
      "body": "GOAL goal-app-preview minted (human-directed, 2026-06-17). Tier=tdd: state machine + state mutation + bounded buffer + URL parsing. Exercises the close-to-done gate when looped. Deps: none. To be dogfood-driven via /hivemind-beta:loop after context clear."
    },
    {
      "author": "orchestrator",
      "at": "2026-06-17T04:20:00.000Z",
      "body": "CLOSED done via the goal-app-preview loop drive (iteration 2, auto_close_on_green_review granted). tdd two-phase: TEST a15dfed (20 RED specs, self-contained temp-dir fixtures, orphan-safe afterEach drain) -> IMPL 46427be (src/preview-process.js: createPreviewController + LOG_BUFFER_CAP=200 ring buffer, stopped/starting/running/exited/error state machine, configured-url + scanned-url detection, idempotent stop, restart/double-start child replacement, stderr wired into same buffer, mirrors orchestrator-bridge spawn discipline). Fresh-context review: APPROVE-WITH-FOLLOWUPS, 0 HIGH/2 MEDIUM/2 LOW/2 NIT. IN-BAND FIX 55dfd70: (MEDIUM #1) decoupled starting->running from URL detection so mode:process / no-url children reach running on spawn (was stuck in starting forever — a real AC2 gap for the resolver's process mode); (+LOW) added a stderr-only regression lock. Reviewer cold-verified the fix: APPROVE. Release-tier gate green: npm run test:all 861 passed/5 skipped. DEFERRED to TASK-067 (explicit constraint, see TASK-067 comment): MEDIUM #2 whitespace command parser breaks on quoted/space-bearing argv — TASK-067 feeds real resolver commands in, so it accepts a pre-split argv there. DEFERRED no-ticket: 2 NITs (named KILL_FALLBACK_MS constant; terminal-state field-retention doc note). Commits LOCAL on beta, NOT pushed (auto_push gate ON)."
    }
  ],
  "created_at": "2026-06-17T03:40:58.615Z",
  "updated_at": "2026-06-17T04:20:00.000Z",
  "jira_key": null,
  "verification_tier": "tdd"
}
