{
  "key": "TASK-067",
  "title": "Console preview backend routes — start/stop/restart/status + SSE log stream",
  "description": "BETA-TEST GOAL (goal-app-preview), part 3 of 5. Hardened routes on createBoardServer (src/task-board.js) wiring the resolver (065) + process manager (066) to the console. Depends on 065 and 066.",
  "acceptance_criteria": [
    "New hardened routes on the board server behind the EXISTING Host allowlist + Content-Type 415 + body-cap 413 guards: POST /api/preview/start, POST /api/preview/stop, POST /api/preview/restart, GET /api/preview/status.",
    "GET /api/preview/status returns bounded JSON { state, mode, url (string|null), source, recentLogs (bounded) } sourced from the process manager + resolver (never dumps unbounded logs).",
    "A live channel: GET /api/preview/stream (SSE) emitting incremental log lines + state changes, bounded, and cleaning up its listener on client disconnect (mirror the TASK-051 SSE-unsubscribe fix — no leak).",
    "start resolves config via 065 and launches via 066; when mode=none, start returns a clear 409/400 with the configure hint (no crash).",
    "tests-after: implement then prove by running (start launches a fixture app, status reflects running + url, stop terminates) + hardening parity (415/413/403) + SSE cleanup; minimal regression locks; e2e under tests/e2e/.",
    "Bundled (task-board) change => rebuild dist + release-tier gate green."
  ],
  "status": "done",
  "priority": "high",
  "labels": [
    "beta",
    "goal-app-preview",
    "console",
    "preview"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-065",
    "TASK-066"
  ],
  "linked_commits": [
    "d700b4a",
    "78fe4ed",
    "1b6e5df"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-06-17T03:40:59.336Z",
      "body": "GOAL goal-app-preview minted (human-directed, 2026-06-17). Tier=tests-after: hardened routes over the existing board server, provable by running; minimal regression locks. Bundled (task-board) => release-tier gate. Deps: TASK-065,TASK-066. To be dogfood-driven via /agentic-framework-beta:loop after context clear."
    },
    {
      "author": "orchestrator",
      "at": "2026-06-17T04:21:00.000Z",
      "body": "CONSTRAINT carried from the TASK-066 review (deferred MEDIUM): the TASK-066 preview-process controller parses config.command via a naive whitespace split (no quoted/space-bearing argv handling). TASK-067 wires the resolver (065) output INTO the controller (066), so the realistic commands flow through here (e.g. 'npm run dev', 'vite --port 5173', or a quoted path with spaces). Address it at THIS seam: have the controller accept a pre-split argv (string[]) in addition to a string, and have TASK-067's start route pass a properly-tokenized argv (or document a strict no-quoting contract on the preview_command field). Confirm a command with a space-bearing path or quoted arg launches correctly in an e2e."
    },
    {
      "author": "orchestrator",
      "at": "2026-06-17T04:45:00.000Z",
      "body": "CLOSED done via the goal-app-preview loop drive (iteration 3, auto_close_on_green_review). tests-after single IMPL phase: src/task-board.js gains hardened POST /api/preview/start|stop|restart + GET /api/preview/status + GET /api/preview/stream (SSE), behind the existing Host-allowlist/415/413 guards; wires resolver (065) + process controller (066); mode=none -> 409 + configure hint; dist/task-board.cjs rebuilt. CARRIED CONSTRAINT resolved: controller now accepts command as string|string[]; route tokenizes; space-bearing path locked. Fresh-context review #1 (d700b4a): REQUEST-CHANGES, 1 HIGH + 2 MEDIUM. HIGH (AC3): SSE emitted only state-changes+snapshot, never incremental log lines -> looped back (reviewer retry 1/2). FIX 78fe4ed: added controller subscribe(cb) hook wired into per-line pushLog + every state transition; /stream subscribes on connect, unsubscribes on socket close (TASK-051 pattern); Lock 9 asserts a post-connect log line (emitted via the fixture's own control endpoint, no board route) reaches the SSE client. MEDIUM source-always-null: plumbed config.source through controller getStatus + resolver fallback when stopped (locked none+configured). MEDIUM weak leak-lock + LOW dead scaffolding: in-band cleanup 1b6e5df added getSubscriberCount() seam and rewrote Lock 6 to assert subscriber count +1 on connect / back to baseline on disconnect (would fail without the route unsubscribe). Re-review 78fe4ed: APPROVE-WITH-FOLLOWUPS (0 HIGH/0 MEDIUM); the 2 residual test-quality LOWs were then fixed in 1b6e5df. Release-tier gate green: npm run test:all 875 passed/5 skipped, dist-parity holds, no orphan. Commits LOCAL on beta, NOT pushed (auto_push gate ON)."
    }
  ],
  "created_at": "2026-06-17T03:40:58.710Z",
  "updated_at": "2026-06-17T04:45:00.000Z",
  "jira_key": null,
  "verification_tier": "tests-after"
}
