{
  "key": "TASK-162",
  "title": "coerceFrontmatterScalar naive comma/colon split mis-parses inline-object values containing commas (perfil_proyecto/agent_models)",
  "description": "CORRECTNESS (not security) — surfaced by the TASK-161 developer + gating reviewer, both independently reproduced. src/project-md.js#coerceFrontmatterScalar parses SPECIAL_FRONTMATTER_IDS inline-object frontmatter (`{k: v, k: v}`) with a naive split on ',' then ':'. A map VALUE (or key) containing a literal comma or brace is mis-parsed: e.g. a perfil_proyecto value 'bold, sleek' passes the TASK-161 control-char guard and writes fine, but readProjectMd then THROWS on read-back (`perfil_proyecto entry \\\"sleek\\\" is missing a colon`). The failure mode is a LOUD read-time throw, NOT a silent forged top-level key (control chars are the injection vector that TASK-157/161 close; commas only mangle within the map) — so this is a robustness/correctness bug, not a security gap.\\n\\nCurrently LATENT: perfil_proyecto values come from deriveProfileFields (controlled profile tags, no commas today) and agent_models values are model aliases/IDs (no commas). It becomes a real bug only if either map's values ever admit free-form text with commas/braces. Minted LOW to track rather than lose the finding.\\n\\nROOT CAUSE: src/project-md.js#coerceFrontmatterScalar inline-object branch (the `raw.slice(1,-1).split(',')` then `indexOf(':')` logic, ~lines 514-530) assumes no literal commas/colons/braces inside keys/values. The writer side (renderProjectMd) also does a naive `${k}: ${v}` join with no escaping. FIX DIRECTION: either escape commas/braces symmetrically on write+read (like the inline-array encodeArrayItem/decodeArrayItem scheme already in this module), or constrain+validate perfil_proyecto/agent_models values to a no-comma charset at write. Prefer the escape scheme for consistency with the existing array handling. verification_tier tdd if an escape scheme is added (parsing change); tests-after acceptable if it is a pure validation-reject.",
  "acceptance_criteria": [
    "A perfil_proyecto (or agent_models) map value containing a literal comma round-trips losslessly through writeProjectMd -> readProjectMd (no read-time throw, no split into extra pairs), OR is rejected at write with a loud field-naming error if the map is intentionally constrained to a no-comma charset",
    "The chosen approach (escape-on-write/unescape-on-read, or validate-and-reject) is symmetric between renderProjectMd and coerceFrontmatterScalar so writer and reader cannot drift",
    "A regression test round-trips a comma-bearing inline-object value (and a brace-bearing one) through the real writer/reader",
    "Existing design-profile-frontmatter / design-profile-round-trip / agent-models-config e2e specs still pass; legitimate no-comma maps are unaffected"
  ],
  "status": "done",
  "priority": "low",
  "labels": [
    "correctness",
    "init",
    "parsing",
    "follow-up"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-161"
  ],
  "linked_commits": [
    "00cbdba"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-07-14T18:57:50.132Z",
      "body": "CLOSED on green review. Reviewer verdict APPROVE (review_depth=full; rubric inputs: parsing change to schema-adjacent inline-object frontmatter — writer renderProjectMd + reader coerceFrontmatterScalar; symmetric writer/reader correctness risk; bundles into two dist entrypoints). 0 HIGH / 0 MEDIUM / 2 LOW (deriveProfileFields reject-guard now over-conservative + stale comment — follow-up minted TASK-164; pre-existing leading/trailing-whitespace trim, out of scope). Clean first-pass, no RC loop — no knowledge draft.\n\nImplementation (commit 00cbdba, tdd single-commit): approach = escape-on-write/unescape-on-read, mirroring the existing inline-ARRAY scheme (encodeArrayItem/decodeArrayItem). New encodeMapEntry (escapes \\ first, then , { }), decodeMapEntry (single L-to-R reverse), splitInlineMapPairs (escape-aware top-level comma split). Applied SYMMETRICALLY: renderProjectMd encodes every key+value for agent_models AND perfil_proyecto; coerceFrontmatterScalar splits+decodes both per pair — reviewer confirmed no drift path and verified the adjacent-escape edge case (literal backslash + comma round-trips losslessly). encodeMapEntry is a no-op on clean input so legitimate no-comma maps ({reviewer: fable}, deriveProfileFields output) write byte-identically (regression guard 66/66; dist-parity byte-match). Reviewer independently reproduced the red in-memory: comma value throws 'entry \"sleek\" is missing a colon' pre-fix (genuine red); brace-only case was green pre-fix too (naive parser only split on comma; braces never an internal delimiter — kept as AC3-mandated regression lock, honestly disclosed by developer). dist/init.cjs + dist/pack-ctl.cjs rebuilt+committed (pack-ctl tree-shakes the writer, parity-validated), dist-parity green. Gate: test:all 1827 passed / 4 skip; reviewer re-ran test:since -- b861675 (73) + fast tier (993) + named e2e (51), all green."
    }
  ],
  "created_at": "2026-07-13T16:22:11.596Z",
  "updated_at": "2026-07-14T18:57:50.132Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
