{
  "key": "TASK-065",
  "title": "App-preview config resolver (PROJECT.md preview block + package.json inference)",
  "description": "BETA-TEST GOAL (goal-app-preview), part 1 of 5. A resolver that determines HOW to start/locate the project app for the console preview. Reads an optional preview block from PROJECT.md (preview_command, preview_url or preview_port, optional preview_mode web|process); if absent, infers from package.json scripts (precedence dev > start > serve) plus a port heuristic. Returns a normalized config. Backward-compatible: no config AND no inferable script => mode none (console shows a configure hint). Built on beta; first ticket of the app-preview goal to dogfood-drive via /hivemind-beta:loop.",
  "acceptance_criteria": [
    "New module src/preview-resolver.js exports resolvePreviewConfig({repoRoot}) returning { mode (enum web|process|none), command (string|null), cwd, url (string|null), source (configured|inferred|none) }.",
    "Configured path: an optional preview block in PROJECT.md frontmatter (preview_command + preview_url or preview_port + optional preview_mode) is parsed and TAKES PRECEDENCE over inference; preview_mode defaults to web when a url/port is present, else process.",
    "Inference fallback: with no preview config, infer command from package.json scripts in precedence dev > start > serve; mode web if a port/url can be derived else process; source=inferred.",
    "No config and no usable script => { mode: none, source: none } with no throw.",
    "state/PROJECT.schema.json extended with the OPTIONAL preview fields; existing PROJECT.md files remain valid (backward-compatible).",
    "tdd: failing tests first encoding precedence (configured > inferred > none), mode derivation, and backward-compat; pure-logic specs at tests/ top level, any disk-fixture reads under tests/e2e/.",
    "Schema change => release-tier gate: npm run test:all green; rebuild dist via npm run build:plugin and commit if any bundled source changes."
  ],
  "status": "done",
  "priority": "high",
  "labels": [
    "beta",
    "goal-app-preview",
    "console",
    "preview"
  ],
  "assignee": null,
  "depends_on": [],
  "linked_commits": [
    "12a8ba9",
    "5c3d1ae",
    "3faed20"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-06-17T03:40:59.112Z",
      "body": "GOAL goal-app-preview minted (human-directed, 2026-06-17). Tier=tdd: source logic + parsing + precedence + PROJECT.md schema change. Release-tier gate (schema). Deps: none. To be dogfood-driven via /hivemind-beta:loop after context clear."
    },
    {
      "author": "orchestrator",
      "at": "2026-06-17T04:05:00.000Z",
      "body": "CLOSED done via the goal-app-preview loop drive (iteration 1, auto_close_on_green_review granted this session). tdd two-phase: TEST 12a8ba9 (23 RED specs, module-not-found = right reason) -> IMPL 5c3d1ae (resolvePreviewConfig + 4 optional preview_* fields in PROJECT.schema.json, additionalProperties:false preserved, dist/init.cjs rebuilt since schema is bundled). Fresh-context review: APPROVE-WITH-FOLLOWUPS, 0 HIGH/1 MEDIUM/3 LOW. The MEDIUM (preview_mode passed through without enum validation -> mode:'garbage' on hand-authored input) was fixed IN-BAND (blast-radius: feature's core input path + TASK-067 will feed mode into routes) at 3faed20 (clamp invalid preview_mode to url/port-derived default; +2 e2e locking specs). Reviewer cold-verified the fix: APPROVE. Release-tier gate green: npm run test:all 840 passed/5 skipped. DEFERRED (no follow-up ticket): LOW duplicate frontmatter parser (bespoke extractFrontmatter vs readProjectMd) and LOW fast-tier AC4 tier/duplication nit. Commits LOCAL on beta, NOT pushed (auto_push gate ON)."
    }
  ],
  "created_at": "2026-06-17T03:40:58.507Z",
  "updated_at": "2026-06-17T04:05:00.000Z",
  "jira_key": null,
  "verification_tier": "tdd"
}
