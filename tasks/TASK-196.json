{
  "key": "TASK-196",
  "title": "The documented UAT recording convention no longer produces a comment the loop-mode gate accepts",
  "description": "Surfaced by TASK-186's fix round as an immediate consequence of its own change. **This is a live doc-vs-code contradiction, not a cosmetic one** — following the documented procedure verbatim now produces a UAT comment that the close gate rejects.\n\n**What changed.** TASK-186 made the structured per-step verdict convention MANDATORY in loop mode: `hasExplicitHumanVerdictMarker` now unconditionally evaluates `evaluateStructuredStepVerdicts`, and a body with no literal `Verdict:` label on any step is rejected outright. The label-free legacy fallback is gone.\n\n**What the docs still say.** `skills/orchestrator-routing/SKILL.md` (and its `.claude/skills/` parity copy), UAT procedure step 3 around lines 452-455, describes the recording convention only as \"per-step verdict ... overall result (PASS or FAIL)\". It never mandates the literal `Verdict:` label. CLAUDE.md's UAT step has the same shape.\n\n**The consequence.** A human or Orchestrator following the documented procedure would naturally write `1. Run X, expect Y. Observed: Y. PASS` — which now FAILS Gate 2 in loop mode and forces a `uat_delegated_to_orchestrator` grant that should not have been needed. The UAT path is documented one way and enforced another.\n\n**This is the third instance of the same pattern in two days,** and that is the reason this is `high` rather than `medium`: TASK-181 changed reconcile-apply's behaviour and left `commands/design-pack.md` describing the old one; TASK-183 changed it again and made TASK-184's freshly-corrected doc stale within two minutes; now TASK-186 changes the UAT contract and leaves the procedure that generates UAT comments describing the old format. Each time, the code was right and the instructions that drive humans and agents were wrong.\n\n**Fix direction.** Update the UAT-recording convention in both `SKILL.md` parity copies (byte-identical) and in `CLAUDE.md`'s Workflow UAT step so the documented format is exactly what the gate accepts: a literal `Verdict: PASS` / `Verdict: FAIL` per step, plus the overall-result line. Show a correct worked example, since the format is now load-bearing rather than stylistic.\n\n**Also worth deciding here:** whether the gate should accept the previously-documented bare form as well. TASK-186 chose strictness deliberately and measured it as corpus-safe, so loosening it would undo that — but the docs and the gate must agree one way or the other, and right now they do not.\n\nNote also that `tests/e2e/close-guard.spec.js`'s `DELEGATED_UAT_COMMENT`, `OVERALL_FAIL_UAT_COMMENT` and `FAIL_BUT_PASS_ON_RETRY_UAT_COMMENT` fixtures still use bare \"verdict PASS\" prose without the colon. Their assertions are all rejections that hold regardless (a delegation marker or FAIL token short-circuits before the structured check), so this is not a functional gap — but the fixtures now model a format the docs should no longer teach.",
  "acceptance_criteria": [
    "A failing doc-lock spec first demonstrates the contradiction: the UAT recording convention documented in skills/orchestrator-routing/SKILL.md does not describe a format that hasExplicitHumanVerdictMarker accepts. Capture the red-run output.",
    "The documented UAT recording convention in BOTH skills/orchestrator-routing/SKILL.md parity copies (byte-identical) and in CLAUDE.md's Workflow UAT step describes exactly the format the loop-mode gate accepts, including the literal per-step 'Verdict: PASS' / 'Verdict: FAIL' label and the overall-result line, with a correct worked example.",
    "A doc-lock pins the correspondence so it cannot silently drift again — ideally by deriving the accepted format from the real guard code rather than restating it, so a future change to the guard breaks the lock. Prove non-vacuity by mutation: change the documented example to a form the gate rejects and confirm the spec fails.",
    "The open question is decided and recorded in the closing comment: does the gate keep TASK-186's strict mandate (docs change to match), or does it also accept the previously-documented bare form (code loosens to match)? Do not leave docs and gate disagreeing.",
    "If the strict mandate is kept, verify no already-closed ticket is retroactively affected — TASK-186 established that close-time guards never re-gate a closed ticket and that exactly one real ticket (TASK-133, already done) relied on the label-free path. Re-verify that count against the current corpus rather than trusting the recorded figure.",
    "tests/e2e/close-guard.spec.js's bare-'verdict PASS' fixtures are either updated to the documented format or explicitly left with a comment stating why they intentionally model a rejected shape.",
    "npm run test:all is green; no existing close-guard or UAT spec regresses; dist rebuilt and committed if any bundled source changes."
  ],
  "status": "done",
  "priority": "high",
  "labels": [
    "docs",
    "uat",
    "close-guard",
    "review-followup",
    "silent-failure"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-186"
  ],
  "linked_commits": [
    "1a925ec",
    "929fa69",
    "ba4cd31"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-08-03T03:25:46.329Z",
      "body": "**SCOPE ADDENDUM from the TASK-186 rounds (now closed, commits `1cf6fa6`/`3f42183`/`ae63934`/`9f942d9`).** This ticket got materially bigger, and its priority should be read as blocking rather than routine.\n\n## The accepted grammar accumulated FOUR undocumented requirements\n\nTASK-186 hardened the loop-mode UAT gate across three rounds. Each round tightened the accepted body shape, and **none of the tightenings reached `SKILL.md` or `CLAUDE.md`.** As committed, a comment satisfies `hasExplicitHumanVerdictMarker` only if all of the following hold:\n\n1. **Every step carries a literal `Verdict:` label** — `STRICT_STEP_VERDICT_RE = /verdict\\s*:\\s*(pass|fail)\\.?\\s*$/i`, anchored at the end of the block. `verdict PASS` and `Verdict = PASS` do NOT satisfy it.\n2. **No preamble at all** — not even a `UAT script:` header line. The \"one leading header line\" exemption was considered and deliberately rejected, because the header is precisely the channel a failure disclosure hides in (probe R3).\n3. **Recognized step-block count >= `acceptance_criteria.length`** — a floor, not coverage (duplicate numbering satisfies it; see LOW below).\n4. **A strict overall line** — `STRICT_OVERALL_RE = /^overall(?:\\s+result)?\\s*:\\s*pass\\.?$/i`, anchored at BOTH ends. Any trailing prose rejects the whole body.\n\n## Why this is blocking, not cosmetic\n\nIndependently confirmed by two separate reviewers against the real `tasks/` directory: **`hasExplicitHumanVerdictMarker` returns `false` for all 45 historical uat comments.** Their bodies use conventions like `Verdict = PASS` and `STEP 1 (...)` that the strict rules reject.\n\nThe consequence is not theoretical: **an orchestrator following the currently-documented convention verbatim cannot produce a comment the gate accepts.** `SKILL.md:452-455` says to record a per-step verdict and state the overall result — it never mandates the `Verdict:` label, forbids a preamble, or specifies the overall-line form. The only working loop-mode UAT paths today are (a) the undocumented strict format, or (b) the `uat_delegated_to_orchestrator` grant.\n\nThis is **fail-CLOSED** — it blocks legitimate closes rather than admitting bad ones, which is the only reason deferring it past TASK-186 was defensible. Nothing unsafe has shipped. But the documented process is currently wrong, and the gap widened with every TASK-186 round.\n\n## What this ticket must now do\n\n**Derive the full accepted grammar from the guard, not just the label.** The doc-lock should pin the complete shape — `<body> ::= <step-block>+ <overall-line>` with every component's exact form — so a future tightening of `src/close-guard.js` that is not mirrored in the docs fails the lock. A doc-lock that checks only for the word `Verdict:` would re-open this exact gap on the next tightening.\n\nFold in these two LOWs from the round-3 review:\n\n- **`src/close-guard.js:173`** — `STRICT_OVERALL_RE` makes `result` optional, so bare `Overall: PASS` is accepted. That is a widening past the documented `Overall result:` form, and the `Overall: PASS` accept branch currently has **no committed positive lock**. Either document both forms or narrow the regex and lock whichever survives.\n- **`src/task-store.js:435-436`** — the widened `OVERALL_FAIL_RE` matches bare prose such as \"no overall failures were observed\", which would false-deny a passing UAT in harness mode. Zero corpus hits today; fail-closed. Note the tradeoff wherever the harness-mode convention is documented.\n\n## Worked example to publish\n\nThe round-3 review's positive control is a body known to be accepted end-to-end through the real composed path. Use a form of it as the documented canonical example, and make the doc-lock assert that the published example actually satisfies the live guard — a documented example that the code rejects is how this drift started.\n\nAlso worth documenting: **how harness mode differs from loop mode.** TASK-186's AC6 required recording that distinction and it landed only in code and spec comments, never in the canonical doc."
    },
    {
      "author": "orchestrator",
      "at": "2026-08-03T04:54:24.751Z",
      "body": "**REQUEST-CHANGES** on independent review of `1a925ec`. Briefed LIGHT, **escalated to FULL by the reviewer** on the stated trigger (documentation misdescribing what a guard accepts). Ticket returns to `in_progress`.\n\n**Findings: 1 HIGH / 3 MEDIUM / 2 LOW.** The fixes are small — a two-word edit plus a few lines — and should all ride one round.\n\n## HIGH — this ticket shipped the exact drift class it exists to close\n\n`skills/orchestrator-routing/SKILL.md` ~line 504 (first \"documented tradeoffs\" bullet), mirrored in `.claude/skills/orchestrator-routing/SKILL.md`.\n\nThe bullet says **\"The AC-count check (both modes) is a floor...\"**. That is false: **harness mode has no AC-count check at all.** `hasRecordedUatVerdict` (`src/task-store.js:463-472`) does no step counting whatsoever. The floor exists only in loop mode's Gate 2 (`evaluateStructuredStepVerdicts`, `src/close-guard.js:310`).\n\nVerified empirically by the reviewer: a 1-step strict-format comment on a **3-AC** ticket returns `true` in harness mode.\n\nIt errs in the dangerous direction — it claims a mechanical sensor exists where none does, inviting reliance on it. **Failure scenario:** a harness-mode operator under-scripts a UAT (1 step, 3 ACs), believes the count floor will catch the shortfall, and the ticket closes.\n\nNote `CLAUDE.md`'s copy is **correct** — it scopes all four requirements to \"the loop-mode gate\". Only the SKILL.md pair is wrong.\n\n**Fix:** `\"both modes\"` → `\"loop mode's Gate 2\"`, then rebuild byte-identical parity. The doc-lock does not catch this because the error is prose *outside* the example block — which is itself worth noting as a limit of the lock's reach.\n\n## MEDIUM\n\n1. **`tests/e2e/close-guard.spec.js:245-259` — the new AC6 comment misstates the deciding mechanism for two of the three fixtures.** It claims their rejections are decided by a check running \"BEFORE (and independently of) the strict per-step `Verdict:` grammar\", attributing `OVERALL_FAIL_UAT_COMMENT` and `FAIL_BUT_PASS_ON_RETRY_UAT_COMMENT` to the FAIL-token backstop. In the code as it stands: `FAIL_VERDICT_RE` runs **after** the per-block strict-grammar loop (`src/close-guard.js:317` vs `:312-316`), and both bodies — written as bare `verdict PASS`, no colon — are rejected at the block loop (`:314`) and never reach the backstop. So both fixtures **do** reach the structured check, contrary to the comment. Only the `DELEGATED_UAT_COMMENT` claim (delegation short-circuit at `:371`) is accurate.\n\n   The load-bearing conclusion survives — relabelling would still reject via `:315`/`:317`, no assertion flips, and the strict-format FAIL paths are independently locked at spec lines 1059-1068, so there is **no coverage hole**. But AC6's deliverable was a comment stating *why*, and the stated why is wrong. **Suggested rewording:** \"their rejections hold under any labelling: as written they fail the strict per-step grammar; relabelled to the documented convention they would still be rejected by the per-step FAIL branch and the `FAIL_VERDICT_RE` backstop.\"\n\n2. **`tests/uat-doc-example-lock.spec.js` — the documented tolerances carry no positive lock.** Both docs now state the guard tolerates the short `Overall: PASS` form and an optional trailing period. Both claims are accurate today (reviewer verified against the live guard), but the lock pins only the canonical example, which exercises neither. The scope addendum had already flagged the `Overall: PASS` accept branch as having no committed positive lock, and orchestrator decision 2 explicitly anticipates a future `tdd` ticket narrowing `STRICT_OVERALL_RE` — which would **silently falsify the published docs**. **Fix, cheap and still derived-from-doc rather than grammar-re-encoding:** two positive assertions that apply the short-form and trailing-period transforms to the extracted example and assert acceptance.\n\n3. **Process — hand-off not relayed.** DISCHARGED: the hand-off exists and states all five R3 checklist items with outcomes, plus the AC1 red-run verbatim. The reviewer reproduced the red state independently anyway (at `dcd893b` the SKILL.md contains **0** `UAT-WORKED-EXAMPLE` markers, so every test throws in `extractWorkedExample` — red for exactly the documented reason). No verdict impact.\n\n## LOW\n\n1. **`CLAUDE.md:52-59` is a second, unlocked copy of the worked example** — the doc-lock reads only `skills/orchestrator-routing/SKILL.md`. Identical today; drift in CLAUDE.md's copy would go undetected. Either declare SKILL.md canonical and replace CLAUDE.md's duplicate with a pointer, or add a cross-check.\n2. **`skills/orchestrator-routing/SKILL.md` ~line 494** — \"the body must be non-empty and name a recognizable PASS/FAIL verdict word\" slightly overstates: a body naming only FAIL never satisfies the harness guard (it rejects, correctly). \"name a PASS verdict and record no FAIL shape\" would be exact.\n\n## What is verified good — do not redo\n\n- **The doc-lock's core design is sound and correctly built.** It reads the doc live from disk, calls the live guard, and re-encodes no grammar. Non-vacuity confirmed by live simulation: markers absent / reordered / START-only / empty block **all throw loudly**; the published example is accepted at 2 ACs and rejected at a 3-AC floor; all three mutations (stripped label, preamble, overall-line trailing prose) are rejected by the real `hasExplicitHumanVerdictMarker`.\n- **Durability property confirmed:** the lock would survive any future tightening of `src/close-guard.js` that rejects the canonical published example. Its blind spots are exactly (a) tolerances outside the example (MEDIUM-2), (b) CLAUDE.md's duplicate (LOW-1), and (c) prose outside the marker block — which is where the HIGH lives.\n- **Parity byte-identical**, computed independently: both copies `abe5d76fed298eee2c90feb90a936f560f9b96d327d864449af9c35ca24b3b1e`.\n- **AC5 corpus re-measured independently:** 191 tasks, 45 uat comments, `hasExplicitHumanVerdictMarker` false for all 45, `hasRecordedUatVerdict` true for all 45, all 45 already `done`.\n- **Scope discipline held:** no `src/`, no `dist/` in the diff, per orchestrator decision 2. Tier `tests-after` defensible.\n- **Gate green:** `test:since -- dcd893b` 2 files/49 tests (real selection, no zero-selection marker) ⬛ `npm test` 82 files/1094 passed ⬛ doc-lock + compat 9 tests ⬛ `close-guard.spec.js` 44 tests ⬛ `test:all` 178 files/2042 passed ⬛ `check:calibration` clean on all 5 changed files."
    },
    {
      "author": "orchestrator",
      "at": "2026-08-03T05:18:10.318Z",
      "body": "**REQUEST-CHANGES (round 2)** on FULL-depth review of `929fa69`. **0 HIGH / 1 MEDIUM / 3 LOW.**\n\n**Depth note:** the rubric computed LIGHT (60+/21-, docs + parity mirror + two specs, no `src/`/`dist/`). The Orchestrator escalated to FULL deliberately, because round 1 of this ticket shipped a HIGH doc-vs-code misdescription on this exact surface that a light briefing caught only via reviewer escalation. Recording the reason so the depth call is auditable.\n\n## Orchestrator ruling: NOT overriding, despite no HIGH\n\nThe reviewer explicitly noted this verdict is a recommendation the Orchestrator may override by accepting the MEDIUM, and recommended against overriding. **Concur.** AC2 requires the documented format to describe *exactly* what the loop-mode gate accepts. Closing a doc-accuracy ticket on a known-false guard-behavior claim in the canonical convention block would be closing it on precisely the defect it exists to eliminate. The fix is two sentences plus a parity rebuild and a doc-lock re-run.\n\n## MEDIUM — one false guard-behavior claim remains\n\n`skills/orchestrator-routing/SKILL.md:469-470` and its `.claude/` mirror: *\"the guard also tolerates the shorter `Overall: PASS` / `Overall: FAIL`\"*.\n\nThe **FAIL half is false in every sense**: `STRICT_OVERALL_RE` (`src/close-guard.js:173`) hardcodes `pass` and never matches `fail`, and any FAIL body is rejected earlier regardless (`:315`/`:317`/`:319`). The guard does not \"tolerate\" `Overall: FAIL` in any form — it rejects all four FAIL spellings identically.\n\n**Echo of the same imprecision** at `SKILL.md:821-825` (Gate 2): *\"it parses in full as `<step-block>+ <overall-line>` ... every step ending with a literal `Verdict: PASS`/`Verdict: FAIL` label\"* reads as marker-satisfying for a `Verdict: FAIL`-labelled body, but the marker returns false for any FAIL (`:315`).\n\n**Failure scenarios:** a loop-mode operator reads Gate 2, believes a fully-grammatical FAIL-recording comment carries the marker, and cannot explain the blocked close; or a future test author adds a positive `Overall: FAIL` acceptance lock that fails on arrival. Both fail-CLOSED — no unsafe close is possible — which is why this is MEDIUM rather than HIGH.\n\n**Suggested fix:** scope the tolerance to PASS — e.g. *\"(the guard also tolerates the shorter `Overall: PASS`; the FAIL spellings are the recording convention for a failed UAT — the marker never accepts any FAIL body, and the ticket returns to the Developer)\"* — and in Gate 2 add *\"with every verdict PASS\"* after the label clause.\n\n## The developer's judgment call — partially upheld\n\nThe developer flagged this area honestly and argued the PASS/FAIL wording documents the *writing convention* rather than guard acceptance. **The reviewer upheld that for the convention sentence** (the convention must cover FAIL outcomes, and step 4 plus \"A failed step sends the ticket back\" makes the consequence unambiguous) **but not for the parenthetical**, which is a literal claim about guard behavior. Correct distinction — the honest flag is what got it adjudicated rather than missed.\n\n## The weak plant was NOT accepted — reviewer supplied the missing proof\n\nThe developer could not prove the new tolerance locks red by reverting `src/close-guard.js` (the environment blocked execution while that file was dirty, the no-`src/` constraint enforcing itself) and inverted each assertion in-memory instead. The reviewer correctly judged that **insufficient** — it proves the assertion executes and can fail, not that it is coupled to the guard's real behavior — and ran the missing experiment in a temp sandbox: copied the marker logic byte-identical except for narrowing `STRICT_OVERALL_RE` to `/^overall\\s+result\\s*:\\s*pass$/i` (require \"result\", forbid period — the exact narrowing orchestrator decision 2 anticipates).\n\n| body | un-narrowed control | narrowed guard |\n|---|---|---|\n| canonical example | true | true |\n| short `Overall:` form | true | **false** |\n| trailing-period form | true | **false** |\n\nBoth locks bite on precisely the anticipated narrowing, and only they do — the canonical lock stays green, correctly isolating the tolerance claims.\n\n**Process note worth carrying:** assertion inversion alone is not adequate plant evidence for a lock whose value is coupling to external behavior. Prove the coupling.\n\n## LOW\n\n1. `tests/uat-doc-example-lock.spec.js:99-104` — the justifying comment says the docs claim the guard tolerates \"an optional trailing period on the overall line\", but the docs claim the period tolerance only for the **step label** (`SKILL.md:463`); the overall-line bullet says \"with nothing appended\". The lock itself is correct and valuable; the comment misattributes its doc anchor — the same claim-vs-source class as the MEDIUM, at comment level. **Fold the reword into this fix round.**\n2. `tests/uat-doc-example-lock.spec.js:124-137` — the trailing-period lock partially overlaps the existing positive lock at `tests/e2e/close-guard.spec.js:278`. Mild redundancy, explicitly requested by the prior review and defensible under the derived-from-doc design. No action.\n3. `tests/e2e/close-guard.spec.js:254-259` — the `:314`/`:315`/`:317` line references will rot on any edit above `close-guard.js:320`. Informational; the prior review's own suggested wording included them.\n\n## Verified good this round — do not redo\n\n- **The two round-1 errors are correctly fixed.** The AC-count floor exists only in `evaluateStructuredStepVerdicts` (`close-guard.js:310`, fed from `acceptance_criteria.length` at `:372-374`), so \"loop mode's Gate 2\" is exact; and \"name a PASS verdict and record no FAIL shape\" matches `UAT_VERDICT_WORD_RE` + `VERDICT_FAIL_RE`/`OVERALL_FAIL_RE` precisely.\n- **The MEDIUM-1 comment rewrite is now accurate** — all four mechanism claims verified in source (both fixtures reject at the per-block loop `:314` as written; relabelled, `OVERALL_FAIL_UAT_COMMENT` hits `:315` and `FAIL_BUT_PASS_ON_RETRY` hits `FAIL_VERDICT_RE` at `:317`; `DELEGATED_UAT_COMMENT` short-circuits at `:371`).\n- **LOW-1 canonical-copy decision is clean.** The CLAUDE.md pointer is accurate (`uat-doc-example-lock.spec.js:30` reads SKILL.md, so it genuinely is the pinned copy); CLAUDE.md's remaining four-requirements prose is exactly right and mentions **only PASS forms**, so it does NOT carry the MEDIUM. A repo-wide sweep found **no other convention copy** in README, `commands/`, `agents/`, or other skills.\n- **Parity byte-identical**, hashed independently: both copies `bb4945d2937f0986e0ee7338a8cdc3b5d4bbde339cb8331810e5b68915ba0fc5`.\n- **Scope held:** no `src/`, `bin/`, or `dist/` in the diff. Tier `tests-after` correct.\n- **Gate green:** `test:since -- 2a0db24` 2 files/51 tests (real selection) ⬛ `npm test` 82 files/1096 passed ⬛ doc-lock + compat 11 ⬛ `close-guard.spec.js` 44 ⬛ `test:all` 178 files/2044 passed ⬛ `check:calibration` clean.\n\n## The definitive answer to this ticket's own question\n\n**Exactly one** claim in the documented convention is not implemented by the code: the parenthetical that the guard \"tolerates\" `Overall: FAIL`, plus its softer Gate 2 echo. Every other claim in `SKILL.md:452-511`, the Gate 2/write-guard bullets at `816-862`, and CLAUDE.md's UAT step now corresponds to `src/close-guard.js` and `src/task-store.js` — verified claim-by-claim against source and, where behavioral, empirically. Fix that one and the ticket's premise is discharged."
    },
    {
      "author": "orchestrator",
      "at": "2026-08-03T05:31:23.751Z",
      "body": "APPROVED and closed on a green review — **0 findings at any severity**.\n\n**Review depth across the three rounds:** round 1 briefed LIGHT and **escalated to FULL by the reviewer** on the stated trigger (documentation misdescribing a guard); round 2 briefed FULL **pre-emptively by the Orchestrator**, recording the reason — round 1 had shipped a HIGH doc-vs-code misdescription on this exact surface, so the surface had empirically earned full depth regardless of its ~60-line diff; round 3 briefed LIGHT (49 lines, two doc sentences plus one spec comment) with an escalation trigger that did not fire. Rubric inputs throughout: docs + byte-identical parity mirror + specs, **no `src/`, `bin/`, or `dist/` in any round**.\n\n## The premise question, answered\n\nThis ticket existed because the documented UAT convention and the code that enforces it had diverged. The third review's direct answer:\n\n> **Yes — every claim in the documented UAT convention now corresponds to what `src/close-guard.js` implements, in mechanism as well as outcome.**\n\nVerified claim-by-claim against `STRICT_STEP_VERDICT_RE`, `STRICT_OVERALL_RE`, `FAIL_VERDICT_RE`, `parseUatBody`, `evaluateStructuredStepVerdicts`, and `hasExplicitHumanVerdictMarker`, across the recording-convention bullets (`SKILL.md:458-476`), the harness-vs-loop distinction (492-500), the two documented tradeoffs (502-513), the worked example, the Gate 2 bullet (818-835), and CLAUDE.md's condensed copy.\n\n## AC4 — the open question, decided\n\n**Keep TASK-186's strict mandate; the docs changed to match the code.** Loosening the gate to accept the previously-documented bare form would have re-opened the exact defect class TASK-186 closed across three fix rounds — unlabelled prose tokens are what the FAIL-token arms race was about. The gate is fail-CLOSED (it denies rather than admits) and was measured corpus-safe. A second decision followed from it: **no guard regex was changed in this ticket**, so the optional-`result` widening was documented rather than narrowed — narrowing a security gate deserves its own `tdd` ticket, and doing it inside a docs ticket would have mis-tiered the work. Both decisions held; no `src/` file was touched in any round.\n\n## The deliverable that outlives the doc edit\n\n`tests/uat-doc-example-lock.spec.js` does **not** restate the grammar as its own regexes — that would be a second copy of the contract, free to drift exactly as the docs did. Instead it **extracts the published worked example verbatim from `skills/orchestrator-routing/SKILL.md` and runs it through the live `hasExplicitHumanVerdictMarker`**, then mutates it and asserts rejection.\n\nDurability confirmed by the round-1 reviewer: the lock would survive any future tightening of `src/close-guard.js` that rejects the canonical published example — because the doc's own example stops being accepted. Non-vacuity confirmed by live simulation: markers absent / reordered / START-only / empty block **all throw loudly**; the example is accepted at 2 ACs and rejected at a 3-AC floor; all three mutations (stripped label, added preamble, trailing prose on the overall line) are rejected by the real guard.\n\n**LOW-1 resolution strengthened it further:** `CLAUDE.md`'s duplicate worked example was replaced with a pointer to the canonical copy, leaving **exactly zero unlocked copies** rather than two hand-synced ones. A repo-wide sweep confirmed no other convention copy exists in README, `commands/`, `agents/`, or any other skill.\n\n## Three doc-vs-code errors found and fixed — the pattern is the lesson\n\n| Round | Error | Why it mattered |\n|---|---|---|\n| 1 (HIGH) | \"The AC-count check **(both modes)**\" | `hasRecordedUatVerdict` does no step counting at all; proven by a 1-step comment on a 3-AC ticket returning `true` in harness mode. Claimed a mechanical sensor existed where none did — the dangerous direction. |\n| 2 (folded LOW) | \"name a recognizable **PASS/FAIL** verdict word\" | `UAT_VERDICT_WORD_RE = /\\bpass\\b/i` only ever checks for \"pass\"; a FAIL-only body never satisfies the harness guard. |\n| 3 (MEDIUM) | \"the guard also tolerates the shorter `Overall: PASS` / **`Overall: FAIL`**\" | `STRICT_OVERALL_RE` hardcodes `pass`; FAIL bodies are rejected three separate ways. |\n\nAll three were in prose written **from intent rather than from source**. That is the durable finding, and it is why round 3's brief required reading the code first and then writing the sentence — including verifying the Orchestrator's own suggested wording rather than pasting it on trust.\n\n## Verified good\n\n- **AC5 corpus re-measured independently** (not trusted from the recorded figure): 191 tasks, 45 uat comments, `hasExplicitHumanVerdictMarker` false for all 45, `hasRecordedUatVerdict` true for all 45, all 45 already `done`. TASK-133 remains the sole documented legacy-path exception. No already-closed ticket retroactively invalidated.\n- **AC6 fixture decision:** the three bare-`verdict PASS` fixtures were left in place with an explanatory comment rather than relabelled. The first attempt at that comment stated the wrong mechanism and was corrected; the final version's four mechanism claims were each verified in source (both fixtures reject at the per-block loop `:314` as written; relabelled, `OVERALL_FAIL_UAT_COMMENT` hits `:315` and `FAIL_BUT_PASS_ON_RETRY` hits `FAIL_VERDICT_RE` at `:317`; `DELEGATED_UAT_COMMENT` short-circuits at `:371`).\n- **Parity byte-identical** at every round, hashed independently rather than asserted — final `f63bec48a7beb7146c24d0d2804b541f057de2806d35cecee8f36eb5c666ddcf`.\n- **Final gate:** `test:since -- f30e61d` 7 tests (real selection) ⬛ `npm test` 82 files/1096 passed ⬛ doc-lock + compat 11 ⬛ `close-guard.spec.js` 44 ⬛ `test:all` 178 files/2044 passed ⬛ `check:calibration` clean.\n\n## Process note carried forward\n\n**Assertion inversion is not adequate red-green plant evidence for a lock whose value is coupling to external behavior.** When the developer could not revert `src/close-guard.js` to plant the tolerance locks red (the environment blocked execution while that file was dirty — the no-`src/` constraint enforcing itself), it inverted each assertion in-memory instead. The round-2 reviewer correctly judged that insufficient and supplied the real proof: it narrowed `STRICT_OVERALL_RE` in a sandbox to exactly the future form this ticket's decision 2 anticipates, and confirmed both new locks go red on that narrowing **and only they do**. Prove the coupling, not just that the assertion can fail."
    }
  ],
  "created_at": "2026-08-02T21:07:58.607Z",
  "updated_at": "2026-08-03T05:31:23.751Z",
  "jira_key": null,
  "verification_tier": "tests-after",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
