{
  "key": "TASK-151",
  "title": "Correct stale \"~2s\" fast-tier timing claim (real wall-clock ~7s) across process docs",
  "description": "hive-self-improve run (target: the development process; dimension: performance/doc-accuracy) measured the fast tier at 6.9-7.4s wall-clock, not the \"~2s\" the docs claim in four places: CLAUDE.md:72 (\"pure logic, no real disk I/O (~2s)\"), CLAUDE.md:97 (\"the whole fast tier (~2s)\"), CLAUDE.md:117 (\"fast tier, ~2s\"), and vitest.config.js:4/11 (\"~2s\" / \"keeps the inner loop and deploy smoke at ~2s\"). The \"~2s\" reflects test-EXECUTION time (vitest reports ~2.1s tests / ~2.9s collect / ~0.8s transform); wall-clock incl. vitest startup is ~7s. Because the per-ticket gate runs the fast tier twice (developer hand-off + reviewer re-verify), the stale figure understates per-ticket gate cost ~3x. Also: the cost is dominated by startup/collect which scales with spec-file count (71 fast specs today), so CLAUDE.md:117's \"keeps per-ticket verification time roughly constant regardless of project age\" and vitest.config.js:11's premise are drifting and should be softened to reflect that scoped test selection (test:changed/test:since), not the tier split alone, is what bounds inner-loop time. NOTE (measurement, do NOT act on it as a speedup): parallelizing the fast tier was tested and is WORSE on Windows — pool=threads 15.4s, forks singleFork=false 16.6s vs 7.0s baseline; only --no-isolate helped (~10%, 6.3s) at a shared-module-state correctness risk. So the fix here is doc-accuracy only, not a config change. Pure docs -> uat-only.",
  "acceptance_criteria": [
    "CLAUDE.md's three '~2s' references (lines ~72, ~97, ~117) are corrected to distinguish ~2s test-execution from ~7s wall-clock (or updated to the honest wall-clock figure)",
    "vitest.config.js's comment (~lines 4, 11) no longer claims the split alone 'keeps the inner loop ... at ~2s'; it credits scoped test selection for bounding inner-loop time",
    "CLAUDE.md:117's 'roughly constant regardless of project age' is softened to acknowledge fast-tier startup/collect scales with spec-file count",
    "No vitest config/pool change is made (measurement showed parallelism is ~2x slower on Windows); this ticket is documentation-only",
    "UAT: a reader following the docs would budget the per-ticket gate realistically (~7s x2 fast tier) rather than ~2s"
  ],
  "status": "done",
  "priority": "low",
  "labels": [
    "hive-self-improve",
    "docs",
    "dx"
  ],
  "assignee": null,
  "depends_on": [],
  "linked_commits": [
    "43d438a"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "uat",
      "at": "2026-07-13T03:46:34.978Z",
      "body": "UAT script (one step per AC), overall result: PASS (5/5).\n\n1. AC1 — Read CLAUDE.md's three former \"~2s\" spots (fast-tier bullet, command table, Rules gate line). Expected: each distinguishes ~2s test-execution from ~7s wall-clock. Observed: all three updated as expected. PASS (verified by Orchestrator at the human's request).\n2. AC2 — Read vitest.config.js top comment block. Expected: no longer claims the tier split alone keeps the inner loop at ~2s; credits scoped test selection (test:changed/test:since) for bounding inner-loop time. Observed: exactly that wording. PASS (verified by Orchestrator at the human's request).\n3. AC3 — Check the \"roughly constant regardless of project age\" claim. Expected: softened to acknowledge fast-tier startup/collect scales with spec-file count. Observed: replaced with scoped-selection attribution + explicit \"startup/collect cost still scales with spec-file count\" sentence. PASS (verified by Orchestrator at the human's request).\n4. AC4 — git diff of vitest.config.js shows comments-only, no pool/config behavior change (2 files, 10+/6-, all prose). Expected: documentation-only. Observed: confirmed. PASS (human verdict).\n5. AC5 — A reader following the docs budgets the per-ticket gate realistically. Expected: ~7s ×2 wall-clock, not ~2s. Observed: CLAUDE.md now states \"budget roughly its wall-clock figure twice per ticket\". PASS (human verdict).\n\nNote: agents/developer.md + agents/reviewer.md (and .claude/agents/ mirrors) still carry the old \"(fast tier, ~2s)\" phrasing — out of AC scope, candidate follow-up ticket."
    },
    {
      "author": "orchestrator",
      "at": "2026-07-13T03:46:46.690Z",
      "body": "Closed on UAT 5/5 PASS (uat-only tier — no reviewer spawn, no new specs; review_depth: n/a per tier). Developer commit 43d438a (docs(TASK-151): correct stale ~2s fast-tier timing claim to ~7s wall-clock): CLAUDE.md ×3 spots + vitest.config.js comment block, comments/prose only, zero config behavior change. Gate: npm run test:changed = no specs selected (expected for md-only diff), npm test = 977 passed / 2 skipped incl. all doc-lock and parity sensors; affected-e2e: none (verified — no e2e asserts on repo-root CLAUDE.md/vitest.config.js prose). No sensor pinned the touched phrases, so no lockstep spec edits were needed. Punch list: agents/developer.md:46 + agents/reviewer.md:23 (and .claude/agents/ mirrors) still say '(fast tier, ~2s)' — out of AC scope, candidate follow-up."
    }
  ],
  "created_at": "2026-07-13T02:17:15.182Z",
  "updated_at": "2026-07-13T03:46:46.690Z",
  "jira_key": null,
  "verification_tier": "uat-only",
  "marker": "[EXPLICIT]",
  "source_tier": "T2"
}
