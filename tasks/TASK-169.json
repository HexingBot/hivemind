{
  "key": "TASK-169",
  "title": "KB-GRAPH-3: graph-freshness sensor — fail the gate when a done task has no task-&lt;n&gt; graph node",
  "description": "From the 2026-07-18 intake+KB-graph design review (Q2, recommended path step 3). Nothing keeps knowledge/graph/graph.json synced to tasks/; the graph is already drifting — task nodes stop at task-150 while TASK-160..167 exist on disk. The manual write-at-close protocol is convention, not code. Add a lightweight sensor (in the style of dist-parity / use-case-policy) that fails test:all when a done ticket in tasks/ has no corresponding task-&lt;digits&gt; node in graph.json, converting silent drift into a caught defect.\n\nNOTE: landing this sensor green requires BACKFILLING the currently-missing task nodes for already-done tickets (task-151..167 and any others) so main is green — include that backfill in this ticket.",
  "acceptance_criteria": [
    "A sensor spec fails when a tasks/ ticket with status 'done' has no task-<digits> node in knowledge/graph/graph.json, naming the missing ticket key(s).",
    "The sensor passes when every done ticket has a node; todo/in_progress/blocked/in_review tickets are NOT required to have nodes (only done).",
    "The sensor is red-green planted: proven to fail on a seeded drift fixture and pass when the node is present.",
    "The sensor runs within test:all (release gate) and is documented in the CLAUDE.md testing/sensor list (parity if a doc-lock applies).",
    "As part of landing, the currently-missing task nodes for already-done tickets are backfilled so the sensor is GREEN on main at merge.",
    "tdd: sensor logic + fixtures."
  ],
  "status": "done",
  "priority": "medium",
  "labels": [
    "kb-graph",
    "review-followup"
  ],
  "assignee": null,
  "depends_on": [],
  "linked_commits": [
    "243386ac87edaf959aed123de047bf52871404e8"
  ],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-07-18T20:01:27.361Z",
      "body": "CLOSED (autonomous loop, auto_close_on_green_review) — APPROVE (0 HIGH / 0 MEDIUM / 1 LOW / 1 NIT). review_depth: FULL (718 insertions across 5 files; new release-gate sensor + shared state knowledge/graph + >150 lines). tdd single-commit. Delivered: src/graph-freshness.js (pure findDoneTicketsMissingGraphNodes + taskKeyToNodeId), fast-tier sensor spec tests/graph-freshness.spec.js (7 tests: seeded drift fixture names keys, complete fixture passes, non-done statuses ignored, live-repo assertion), one-time idempotent backfill via the real addNode API (scripts/backfill-graph-task-nodes.mjs) adding exactly 74 task nodes, CLAUDE.md sensor doc (dist-parity style). REVIEW NOTE: first reviewer spawn stalled on infrastructure (watchdog, not a finding) mid id-consistency check; orchestrator settled that question deterministically (pre-existing zero-padded convention preserved: task-001 present/task-1 absent, sensor derives identical padded ids, ZERO semantic duplicates, live check 0 missing) and a fresh reviewer completed the review with the ground truth pre-answered. tdd red-run CREDIBLE: replacement reviewer extracted the parent graph and ran the committed pure function — exactly 74 missing keys, first/last entries matching the spec's RED-GREEN EVIDENCE verbatim. dist claim verified TRUE (graph-freshness bundles into no entrypoint). Backfill hygiene clean (addNode-only, no deletions, idempotent early-return). Gates green: fast tier 1003/2 (sensor now live in the release gate), knowledge-graph e2e 41 tests, calibration clean, doc-locks green. LOW (batch at epic end): a done ticket with a malformed key is silently skipped by the sensor (unreachable today; a malformed-key bucket would make it fail-closed). Commit 243386a."
    }
  ],
  "created_at": "2026-07-18T17:48:58.675Z",
  "updated_at": "2026-07-18T20:01:27.361Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
