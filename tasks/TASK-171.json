{
  "key": "TASK-171",
  "title": "KB-GRAPH-4: auto-create the task node inside close_task's guarded write path",
  "description": "From the 2026-07-18 intake+KB-graph design review (Q2, recommended path step 4). HUMAN SCOPE DECISION (2026-07-18): PULL FORWARD (not deferred), given the drift already observed (task nodes at 150 vs tickets at 167). Make close_task (src/mcp-server.js / src/task-store.js) auto-create the task-&lt;digits&gt; node in knowledge/graph/graph.json as PART of its guarded write, so the node exists by construction at close rather than relying on the orchestrator's manual convention (the root cause of the drift). Keep the decoupling pattern (inject the graph writer at the MCP layer, like the loop-mode guards). Must not weaken close_task's atomic all-or-nothing semantics or its existing guards. This complements KB-GRAPH-3's sensor by removing the drift source. Depends on TASK-168 (shared graph query/write surface).",
  "acceptance_criteria": [
    "Closing a ticket via close_task idempotently creates a task-<digits> node in knowledge/graph/graph.json for that ticket if absent (id = lowercase task-<n>, ref = the task JSON path, label = the ticket title), as part of the same close operation.",
    "Idempotent: re-closing or an already-present node neither duplicates nor errors; a graph-node-write failure does not corrupt the atomic ticket close (ordering documented — e.g. ticket close stays all-or-nothing and the node write is best-effort-after, or both are in one validate-then-write pass per the chosen design).",
    "Existing close_task guards (uat-only done-guard, loop-mode Gate 1/2, invalid-sha atomicity) remain green; the graph write does not bypass or weaken them.",
    "dist rebuilt + committed if bundled src changed; dist-parity green.",
    "tdd: state-mutation on the close path + fixtures."
  ],
  "status": "todo",
  "priority": "medium",
  "labels": [
    "kb-graph",
    "review-followup"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-168"
  ],
  "linked_commits": [],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-07-18T17:57:01.303Z",
      "body": "SCOPE REFINEMENT (human decision 2026-07-18, symmetric with TASK-168's merge-seam choice): create the close-time task node via src/graph-sync.js#recordNode (local projection ALWAYS + best-effort kb_assert mirror to the brain, queued when offline) rather than a bare knowledge-graph.js addNode. This keeps the write side symmetric with 168's merge-aware read side and external-ready.\n\nAC amendments / guardrails:\n- Brain ABSENT (default): recordNode's local addNode is the only effective write — behavior identical to the original 'auto-create task node' scope, zero infra.\n- Brain PRESENT (opt-in): the node also mirrors canonically via kb_assert, best-effort/queued — never blocking.\n- ATOMICITY (critical): only the LOCAL projection write participates in close_task's all-or-nothing semantics; the canonical kb_assert mirror must be best-effort/out-of-band and must NEVER fail or stall the atomic ticket close. A brain that is down/slow cannot break or delay close_task. Prove this by spec (brain injected as absent AND as throwing/queuing).\n- Existing close_task guards (uat-only, loop-mode Gate 1/2, invalid-sha atomicity) remain green and are not bypassed by the graph write.\nNote: recordNode is async and best-effort on the canonical side — keep the decoupling pattern (inject the graph writer at the MCP layer, like the loop-mode guards)."
    }
  ],
  "created_at": "2026-07-18T17:49:24.962Z",
  "updated_at": "2026-07-18T17:57:01.303Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
