{
  "key": "TASK-182",
  "title": "pack-ctl output-contract drift + dual-copy precedence (TASK-181 carry-forward)",
  "description": "Carry-forward batch from TASK-181's gating review (light depth, 2026-08-02) plus the design question TASK-181 deliberately deferred. Three independent items, grouped because they all sit on the same pack-ctl / reconciler surface.\n\n**Item 1 (MEDIUM, from TASK-181 review) — bin/pack-ctl.js header documents an output contract the code no longer honors.**\nThe header at `bin/pack-ctl.js:90-95` states: \"`{ ok: true, ... }` on success (exit 0) or `{ ok: false, code, message }` on failure (exit 1; message also goes to stderr)\". TASK-181 introduced a new total-failure path for `reconcile-apply` that emits `ok: false` with **exit 0** and **no** `code` and **no** `message` — it carries `planned_install_count` / `installed_count` / `source_root` instead. A consumer scripting against the documented contract will mis-parse precisely the failure mode TASK-181's AC6 existed to surface, which is the same class of invisibility that let the original outage survive multiple releases. Decide deliberately whether the CODE should conform to the header (emit code/message, exit non-zero) or the HEADER should document the new shape — do not just paper over one side.\n\n**Item 2 (LOW, from TASK-181 review) — spec env-hygiene inconsistency.**\n`tests/e2e/pack-ctl.spec.js:89-91`'s `runCli` deletes `CLAUDE_PROJECT_DIR` but not `CLAUDE_PLUGIN_ROOT`, while `tests/e2e/pack-ctl-consumer-source.spec.js:70-74`'s `runCli` deletes both. Harmless today — the one test relying on resolution order uses the verbatim `HIVEMIND_OWNED_SOURCE_ROOT` override, which outranks `CLAUDE_PLUGIN_ROOT` — but a future test in that file depending on fallback order could flake when run inside a real plugin session where `CLAUDE_PLUGIN_ROOT` is set in the ambient environment.\n\n**Item 3 (design decision, deferred out of TASK-181) — dual-copy precedence for `watch`, now genuinely live.**\nThis was TASK-181's ORIGINAL scope. It was voided at re-scope time because the premise was false: the project-scope copy was unreachable for consumers, so two copies could never disagree. TASK-181 fixed exactly that, so the premise is now TRUE and the question is live for the first time.\n\nA consumer can now hold `.claude/skills/watch/` (loads as `/watch`, installed via `pack-ctl reconcile-apply`, pinned by `integrations.lock.json`) AND receive the plugin's `skills/watch/` (loads as `/hivemind:watch`, advances with the plugin version). They are byte-identical today. They diverge on the next release that re-pins `watch`: the plugin copy moves, the project copy stays frozen until someone re-runs reconcile. Two near-identically-named skills at different versions of pinned third-party code, with no rule saying which is authoritative.\n\nNote why the obvious fix is wrong: simply dropping the pack resource makes the reconciler compute a REMOVE op against every project whose lockfile already owns `skill:watch`, deleting a working skill out from under existing consumers. TASK-180 avoided this deliberately. Any solution must handle already-installed projects, not just new ones.\n\nRelevant new fact from TASK-181: owned copies are now a SEARCH PATH, not one directory — `src/pack-apply.js#applyPlan` builds `[...new Set([sourceRoot, ...(sourceRoots || [])])]` where `sourceRoot` defaults to `<root>/assimilated-skills` and the plugin root arrives via `sourceRoots`. So the consumer's own staging dir already LEADS the plugin, per-resource, via first-existing-match in `executeInstall`. That is a de-facto precedence rule that is implemented but nowhere documented, and it may or may not be the rule we want. Candidate directions (decide, do not assume): (a) teach the reconciler to skip or actively retire a project-scope resource when the plugin ships the same id at an equal-or-newer pin, releasing the lockfile edge cleanly; (b) keep both channels, document the precedence that already exists, and give consumers a way to see which copy is active and at what pin; (c) make plugin-root shipping the only channel with a one-time migration that retires the project copy without a bare REMOVE.\n\nItem 3 needs a human call on direction before implementation — investigate, propose, then implement the chosen option.",
  "acceptance_criteria": [
    "Item 1: a deliberate decision is recorded (in the closing comment) on whether bin/pack-ctl.js's code conforms to its documented output contract or the header is updated to document the new reconcile-apply shape. Whichever way it goes, the header and the actual emitted shape agree, and a spec asserts the agreement so the two cannot drift again.",
    "Item 1: if the decision is to change the exit code or add code/message, verify no existing caller or spec depends on the current exit-0 behavior before changing it — TASK-181's AC6 spec asserts the ok:false shape and must be updated in step, not broken.",
    "Item 2: tests/e2e/pack-ctl.spec.js's runCli mirrors the env hygiene of tests/e2e/pack-ctl-consumer-source.spec.js:70-74 (deletes CLAUDE_PLUGIN_ROOT as well as CLAUDE_PROJECT_DIR), so no test in that file can be influenced by an ambient plugin-session environment.",
    "Item 3: the precedence rule between a plugin-root-shipped resource and the same resource installed project-scope via integrations.lock.json is decided by the human and recorded in writing (docs/design/addon-packs.md or the pack-descriptor docs), including which copy is authoritative when the pins differ, and whether the de-facto search-path precedence introduced by TASK-181 is the intended rule or an accident to be changed.",
    "Item 3: the chosen behavior is implemented and covered by specs INCLUDING the already-installed-project case — a lockfile that already owns skill:watch must NOT be left with a dangling edge and must NOT silently lose a working skill (a bare REMOVE op against existing consumers is a defect, not a solution).",
    "Item 3: a regression spec proves the divergence scenario end to end — plugin ships watch at pin B while a project lockfile owns watch at older pin A — asserting the documented precedence actually holds.",
    "Item 3: /hivemind:design-pack and any reconcile-facing docs state what a consumer should expect when both copies are present, and how to tell which is active and at what pin.",
    "npm run test:all is green; no existing pack/reconcile/assimilate/apply spec regresses; dist/pack-ctl.cjs rebuilt via npm run build:plugin and committed if bundled source changes, with tests/e2e/dist-parity.spec.js passing."
  ],
  "status": "todo",
  "priority": "medium",
  "labels": [
    "addon-packs",
    "reconciler",
    "watch",
    "review-followup",
    "docs"
  ],
  "assignee": null,
  "depends_on": [
    "TASK-181"
  ],
  "linked_commits": [],
  "linked_prs": [],
  "comments": [
    {
      "author": "orchestrator",
      "at": "2026-08-02T19:21:30.768Z",
      "body": "SCOPE CHANGE after the 2026-08-02 deep review (both passes, 31 confirmed findings total).\n\n**Item 1 (pack-ctl header output contract) MOVES to TASK-183.** The deep review escalated it — pass 2 rated it HIGH — and, more importantly, it is the same code path as TASK-183's `ok`/`installed_count` predicate work. Fixing them in two tickets would mean two passes over `bin/pack-ctl.js`'s reconcile-apply branch and two `dist/pack-ctl.cjs` rebuilds. TASK-183 AC6 carries it, including the new requirement that a spec assert `result.status` and not just `json.ok` (the current spec asserts only the latter, which is why the exit-code gap shipped).\n\n**Item 2 (spec env hygiene, `runCli` not deleting `CLAUDE_PLUGIN_ROOT`) STAYS here.** Note it is now adjacent to TASK-183's rework of `tests/e2e/pack-ctl-consumer-source.spec.js` — sequence after TASK-183 to avoid a collision on the same spec files.\n\n**Item 3 (dual-copy precedence) STAYS here, and the deep review gave it teeth.** It is no longer a purely hypothetical design question: pass 1 proved that the current de-facto rule (local staging dir always leads, selected by a bare `existsSync`) lets an EMPTY `assimilated-skills/<id>/` shadow the plugin's valid copy, materialize an empty skill, and record the empty-tree SHA-256 as a verified integrity at the upstream pin. TASK-183 fixes the validation hole; it does NOT decide the precedence policy. This ticket still owns that decision, and should now be decided with the empty/partial-copy case as a first-class input rather than as a clean two-valid-copies thought experiment.\n\nAdditional confirmed findings from the deep review that belong to neither TASK-183 nor TASK-185 and are folded in here as LOW: `src/mcp-server.js` line ~169 comment cites `tests/e2e/mcp-server.spec.js` as justification for exporting `recordTaskGraphNode`, but the actual lock lives in `tests/e2e/mcp-close-task.spec.js` — the comment misdirects anyone auditing the export. And `HIVEMIND_OWNED_SOURCE_ROOT` is documented as an explicit override yet is outranked by `<repoRoot>/assimilated-skills` in the full `applyPlan` pipeline (it wins inside `resolveOwnedSourceRoot`, then loses because `sourceRoot` leads `searchPath`) — decide whether the documentation or the ordering is wrong."
    },
    {
      "author": "orchestrator",
      "at": "2026-08-02T20:39:31.061Z",
      "body": "SCOPE REDUCTION — verified by the 2026-08-02 cross-ticket audit against HEAD, not inferred.\n\n**ACs 1 and 2 (Item 1, the pack-ctl output contract) are DONE. Strike them.** They were moved to TASK-183 by the earlier scope-change comment, and TASK-183 delivered them at `0a76ae2`: the code was conformed to its own module header (decision recorded in TASK-183's close comment), `main()` now exits 1 with `message` on stderr for any `ok:false` (`bin/pack-ctl.js:515-528`), failure `code`/`message` are emitted (`:480-483`), and the spec asserts `result.status` rather than only `json.ok`. Whoever picks this ticket up should treat only Items 2 and 3 as live.\n\n**Item 2 verified STILL REAL.** `tests/e2e/pack-ctl.spec.js:80-92`'s `runCli` deletes `CLAUDE_PROJECT_DIR` but not `CLAUDE_PLUGIN_ROOT`, while `tests/e2e/pack-ctl-consumer-source.spec.js:72` deletes both.\n\n**Item 3 verified STILL REAL — and now sharper than when it was written.** No precedence-policy document exists anywhere. The de-facto rule is live at `src/pack-apply.js:402` (local `assimilated-skills/` leads, nearest-ancestor-first), and TASK-183 **locked that behaviour with mutation-hardened specs without deciding whether it is the rule we want**. So the ticket now has a concrete contract to either ratify or deliberately change, rather than a blank design question.\n\n**Regression warning for whoever implements Item 3.** If the human's direction is (a) retire/skip the project copy when the plugin ships an equal-or-newer pin, or (c) migrate to plugin-only, then TASK-183's day-old precedence specs WILL fail — correctly, because the contract changed. Those failures must be treated as deliberate contract rewrites with the reasoning recorded, never silenced as noise. And a bare REMOVE op against a lockfile that already owns `skill:watch` would delete a working skill out from under existing consumers, which this ticket already flags.\n\n**The folded LOW is also still real:** `src/mcp-server.js:188` still cites `tests/e2e/mcp-server.spec.js` as the justification for exporting `recordTaskGraphNode`, while the actual lock lives in `tests/e2e/mcp-close-task.spec.js`.\n\n**Status: blocked on a human decision for Item 3 only.** Items 2 and the folded LOWs can proceed independently at any time; the pack surface is file-disjoint from every other queued ticket, so this is a safe parallel candidate."
    }
  ],
  "created_at": "2026-08-02T18:37:52.484Z",
  "updated_at": "2026-08-02T20:39:31.061Z",
  "jira_key": null,
  "verification_tier": "tdd",
  "marker": "[EXPLICIT]",
  "source_tier": "T1"
}
